import StyledFooter from "./Footer.styled";

function Footer() {
  return (
    <StyledFooter>
      <footer>
        <h2>Movie App</h2>
      </footer>
    </StyledFooter>
  );
}

export default Footer;
